# Build / Gradle
*.iml
.gradle/
**/.gradle/
/build
**/build/
.kotlin/
**/.kotlin/
/captures
.externalNativeBuild
.cxx
*.hprof

# Local config (절대 커밋 금지 — 디바이스/사용자별)
local.properties
**/local.properties

# iOS auth secrets (Google OAuth client id + BFF base URL). template 만 커밋.
iosApp/Configs/Auth.xcconfig

# IDE
/.idea
.idea/
*.iml

# macOS
.DS_Store

# Xcode
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
# Xcode device build output (xcodebuild -derivedDataPath build-device)
iosApp/build-device/
# Xcode SwiftPM workspace cache (locally generated)
**/xcshareddata/swiftpm/
