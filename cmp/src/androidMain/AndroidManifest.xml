<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+ PickVisualMedia 호환 + 메타데이터 추출용 -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- 즉시 녹음 기능 — runtime 마이크 권한 prompt -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 음원분리 완료/실패 알림 (API 33+ runtime prompt) — iOS UNUserNotificationCenter 동등 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Android 12 이하 호환 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 9 이하: 갤러리 저장 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".VibiApplication"
        android:label="vibi"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vibi"
        android:usesCleartextTraffic="true">
        <!-- AdMob 앱 ID (프로덕션). 누락/오형식이면 Ads SDK init 이 크래시하므로 반드시 존재해야 함. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4825847811436125~6888367190" />
        <!-- 기본 세로 잠금 (iOS 기본 .portrait 동등). 풀스크린 영상 재생 시에만 LockLandscape 가
             requestedOrientation 을 SENSOR_LANDSCAPE 로 flip 해 가로 허용. configChanges 로 그 회전
             (및 시스템 폰트/다크모드/키보드 변화)에서 Activity 재생성을 막아 스플래시 리셋·상태 유실 방지
             — Compose 가 구성 변경을 반응형으로 재배치한다. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density|fontScale"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
